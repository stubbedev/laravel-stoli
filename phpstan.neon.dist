includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: max
    paths:
        - config
        - src
        - tests
    # The package's own config file is where env() belongs.
    configDirectories:
        - config
